# Business modules

NovaScale is a modular monolith. Each business module will use the boundary:

`api -> application -> domain <- infrastructure`

Modules are added only when their roadmap phase begins. Cross-module shortcuts are not allowed.
